package com.example.application.config.constants

class Constants {
    companion object {
        const val AUTHORIZATION_HEADER = "Authorization"
        const val REFRESH_AUTHORIZATION_HEADER = "Authorization_Refresh"
        const val BEARER_BY_TOKEN_PREFIX = "Bearer"
        const val AUTH_CLAIM_NAME = "authority"
        const val TOKEN_START_INDEX = 7
    }
}